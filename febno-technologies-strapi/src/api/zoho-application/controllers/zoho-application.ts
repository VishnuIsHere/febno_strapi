/**
 * zoho-application controller
 */

import { factories } from '@strapi/strapi'

// @ts-ignore - Types will generate on first successful start
export default factories.createCoreController('api::zoho-application.zoho-application');
